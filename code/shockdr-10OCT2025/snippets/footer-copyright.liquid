{% capture powered_by_link %} {{ 'footer.powered_by' | t }} <a href="https://www.shopify.com/" rel="noopener" target="_blank" rel="nofollow">Shopify</a>.{% endcapture %}
{%- assign bottom_menu = section.settings.bottom_menu -%}

<div class="footer-copyright">
  <div class="footer-copyright__content">
    <span class="footer-copyright__copy">&copy; {{ 'now' | date: "%Y" }}, {{ shop.name | link_to: routes.root_url }}.</span>
     {% comment %}
    <span class="footer-copyright__powered">{{ powered_by_link }}</span>
    {% endcomment %}
  </div>

  {%- if section.settings.accessibility_logo -%}
    <div class="footer-copyright__logo">
      {%- if section.settings.accessibility_url != blank -%}<a target="_blank" href="{{ section.settings.accessibility_url }}">{%- endif -%}
      {{ section.settings.accessibility_logo | image_url: height: 50 | image_tag: height: 25 }}
      {%- if section.settings.accessibility_url != blank -%}</a>{%- endif -%}
    </div>
  {%- endif -%}

  {%- if bottom_menu != blank and linklists[bottom_menu].links.size > 0 -%}
    <div class="footer-copyright__menu">
      {% for link in linklists[bottom_menu].links %}
        <a href="{{ link.url }}" class="footer-copyright__link">{{ link.title | strip_html | escape }}</a>
      {% endfor %}
    </div>
  {%- endif -%}
</div>